# NGINX configuration for Trading
# Copy to: /etc/nginx/sites-available/trading
# Enable: sudo ln -s /etc/nginx/sites-available/trading /etc/nginx/sites-enabled/
# Domain: trading.zitrapps.com

upstream trading_backend {
    server 127.0.0.1:8000 fail_timeout=0;
}

server {
    listen 80;
    server_name trading.zitrapps.com www.trading.zitrapps.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name trading.zitrapps.com www.trading.zitrapps.com;

    # SSL (Let's Encrypt - run: certbot --nginx -d trading.zitrapps.com)
    ssl_certificate /etc/letsencrypt/live/trading.zitrapps.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/trading.zitrapps.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    access_log /var/log/nginx/trading-access.log;
    error_log /var/log/nginx/trading-error.log;

    client_max_body_size 20M;

    # API → Gunicorn
    location /api/ {
        # Handle CORS preflight requests at the Nginx level
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '$http_origin' always;
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, Accept, Origin, X-Requested-With' always;
            add_header 'Access-Control-Allow-Credentials' 'true' always;
            add_header 'Access-Control-Max-Age' 86400;
            add_header 'Content-Length' 0;
            add_header 'Content-Type' 'text/plain';
            return 204;
        }

        proxy_pass http://trading_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Add CORS headers to proxied responses
        add_header 'Access-Control-Allow-Origin' '$http_origin' always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
    }

    # Django static
    location /static/ {
        alias /var/www/trading/backend/staticfiles/;
    }

    # Media uploads
    location /media/ {
        alias /var/www/trading/backend/media/;
    }

    # Frontend (React SPA)
    location / {
        root /var/www/trading/static/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
